# Credit-Card-Fraud-Detection-using-ML
Credit Card Fraud Detection using Machine Learning is a data-driven approach to automatically identify suspicious credit card transactions and reduce financial loss. The core idea is to train an ML model on historical transaction data—where each transaction is labeled as fraud or legitimate—so the model learns patterns.
